[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  # Supabase anon key is safe to expose - it's protected by Row Level Security
  SECRETS_SCAN_ENABLED = "false"
